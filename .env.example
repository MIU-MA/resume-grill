# 服务端配置（部署场景，可选）：三个变量都填则服务端直接用此 Key，前端无需填写。
# 三个变量任一缺失，服务端回落到「前端设置页填入的 Key」；再无则仅「示例简历」可走规则分析，真实简历会提示先配置模型。
# 支持任何 OpenAI 兼容接口：OpenAI、DeepSeek、Moonshot、本地 Ollama 的 OpenAI 兼容端点等。
# 无论哪种来源，API Key 都只经服务端 Route Handler 转发到模型，绝不在浏览器侧直连模型 API。

OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-5.4-mini

# 也可不配此文件，直接在应用首页「模型设置」里填写并保存到浏览器本地（仅个人本地使用）。
