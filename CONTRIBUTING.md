# 参与贡献

感谢你愿意改进 Resume Grill。项目保持“小而专注”：围绕简历声明的证据审计、动态追问和复盘，不扩展为完整求职管理平台。

## 开始之前

1. 搜索现有 Issue，确认问题尚未被讨论。
2. Bug 请提供最小复现；功能建议请说明它如何服务于核心流程。
3. 不要在 Issue、截图、测试或提交中包含真实姓名、联系方式、简历、岗位内部信息或 API Key。

## 本地开发

环境要求：Node.js 20.9 或更高版本。

```bash
npm install
npm run dev
```

提交代码前运行：

```bash
npm run lint
npm test
npm run build
```

## 修改原则

- 优先复用现有类型、组件和数据流，避免引入不必要依赖。
- 模型输出必须经过 Zod 校验；重要状态不能只依赖模型判断。
- 涉及解析、覆盖率、轮次或存储迁移的修改必须添加回归测试。
- 示例数据必须脱敏，优先使用 `examples/` 中的材料。
- 不提交 `.env.local`、浏览器存储、真实模型响应或生成目录。

## Pull Request

PR 描述应包含：问题、实现方式、验证命令，以及涉及界面时的脱敏截图。保持单个 PR 目标清晰，不夹带无关重构。
